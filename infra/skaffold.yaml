apiVersion: skaffold/v4beta9
kind: Config
metadata:
  name: nestjs-microservice
build:
  artifacts:
    - image: keys-api
      context: .
      sync:
        manual:
          - src: /src/**/*.ts
            dest: ./services/keys
      docker:
        dockerfile: ./services/keys/Dockerfile
    - image: web3-api
      context: .
      sync:
        manual:
          - src: /src/**/*.ts
            dest: ./services/web3
      docker:
        dockerfile: ./services/web3/Dockerfile
  local:
    push: false
    concurrency: 1
manifests:
  helm:
    releases:
      - name: ingress
        chartPath: ./infra/helm/ingress
        # skipBuildDependencies: true
      - name: kafka
        chartPath: ./infra/helm/kafka
        namespace: kafka
        createNamespace: true
        # skipBuildDependencies: true
      - name: keycloak
        chartPath: ./infra/helm/keycloak
        namespace: keycloak
        createNamespace: true
        # skipBuildDependencies: true
      - name: postgresql
        chartPath: ./infra/helm/postgresql
        valuesFiles:
          - ./infra/helm/common/values.yaml
        namespace: database
        createNamespace: true
        # skipBuildDependencies: true
      - name: keys-api
        chartPath: ./infra/helm/keys
        valuesFiles:
          - ./infra/helm/common/values.yaml
        setValues:
          image.repository: keys-api
      - name: web3-api
        chartPath: ./infra/helm/web3
        valuesFiles:
          - ./infra/helm/common/values.yaml
        setValues:
          image.repository: web3-api
deploy:
  helm: {}
